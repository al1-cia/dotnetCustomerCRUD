#This simple .NET project demonstrates CRUD.

Clone the repository to Visual Studio.
Connect to MSSql database using SSMS.
Database name is CustomersDB.
The customers table has ID, Name, and isBlocked columns.
